rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Photo upload/finalization is a later increment. Deny access until implemented.
    match /{path=**} { allow read, write: if false; }
  }
}
