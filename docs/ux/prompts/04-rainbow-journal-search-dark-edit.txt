Use case: ui-mockup. Edit the supplied dark glass Gratitude journal board with one layout correction: in M10 the violet 10 September collapsed card and blue 9 September collapsed card MUST have exactly equal outer heights. Use the existing taller violet card as the height reference; extend the blue card downward to the identical height, keeping its View entry button at the same bottom inset. Move the partial cyan card down accordingly; it can be just a visible sliver behind the fixed bottom bar. In M11 make the sole blue search result card exactly the same outer height as those two collapsed cards, preserving its text and bottom-aligned View entry button. All three collapsed cards must have matching heights regardless of body length. Preserve their widths, top positions (except the moved cyan card), colours, exact text, search controls, complete M12 detail screen, background, tabs, and all other layout. No new UI.
